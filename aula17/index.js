function soma(x, y) {
    const resultado = x + y;
    return resultado;
}

const resultado = soma(3, 5);
console.log(resultado);

function imprimir(texto) {
    console.log(texto);
}

const raiz = n => Math.sqrt(n);

imprimir(raiz(16));